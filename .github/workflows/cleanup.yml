name: Delete old workflow runs
on:
  schedule: [cron: '30 10 * * *']
  workflow_dispatch:
    inputs:
      days:
        description: 'Number of days.'
        required: true
    
jobs:
  Cleanup:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 14 || ${{ github.event.inputs.days }}
          keep_minimum_runs: 6